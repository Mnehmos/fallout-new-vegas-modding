# Engine Bug Wiki build notes

Initial static-site build seeded from the GECKWiki `Engine Bugs (Fallout New Vegas)` remaining-bugs list.

The catalog intentionally distinguishes source-derived seeds from active investigations. Current active priorities are VATS projectile targeting, `iSneakLevelBonus`, bulk inventory removal lifecycle, NPC armor enchant stacking, leveled-actor template selection, off-cell AI processing, stuck combat state, crafted-item scripts, projectile water impacts, exterior/Havok warps, looping audio lifecycle, and `SayToDone` off-cell behavior.

The canonical engine knowledge base remains under `research/re/` and is governed by its stricter confidence policy. This public wiki is an investigation front-end, not an alternate symbol authority.
